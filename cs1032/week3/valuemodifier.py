x = 5 # x is not defined, that is the error
if x > 0: # checks if x is greater than 0
    x = x + 1 # increase x by 1
    print("increasing x") # prints than x is increased
else: # if the above if statement is not satisifed
    x = x - 1 # decreases x by 1
    print ("decreasing x") # prints than x is decreased
print("x is processed") # prints that x has been 'processed'